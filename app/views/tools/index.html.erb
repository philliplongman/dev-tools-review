<div class="row">
  <h1>Dev Tools</h1>

  <ul>
    <% @tools.each do |tool| %>
    <li><%= link_to tool.name, tool_path(tool.id) %></li>
    <% end %>
  </ul>
</div>

<%= link_to "Add a dev tool!", new_tool_path %>
