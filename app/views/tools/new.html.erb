<h1>Add a new tool</h1>

<%= form_for @tool do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit "Submit", class: "button" %>
<% end %>

<%= link_to "Cancel", tools_path %>
